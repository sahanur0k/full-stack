<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registrations List</title>
    <!-- Bootstrap for styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f4f6f9;
            font-family: Arial, sans-serif;
        }
        .container {
            margin-top: 20px;
        }
        h2 {
            text-align: center;
            margin-bottom: 30px;
        }
        table {
            border-radius: 10px;
            overflow: hidden;
        }
        th, td {
            text-align: center;
        }
        .btn {
            margin-top: 10px;
        }
        .action-btns {
            display: flex;
            justify-content: center;
        }
        .action-btns form {
            margin-left: 10px;
        }
    </style>
</head>
<body>

<div class="container">
    <h2>List of All Registrations</h2>
    
   
    
    <table class="table table-bordered table-striped">
        <thead class="thead-dark">
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>Email</th>
                <th>Roll Number</th>
                <th>Registration Number</th>
                <th>Semester</th>
                <th>Department</th>
                <th>Event</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {{#each registrations}}
                <tr>
                    <td>{{@index}}</td>
                    <td>{{this.name}}</td>
                    <td>{{this.email}}</td>
                    <td>{{this.roll_number}}</td>
                    <td>{{this.registration_number}}</td>
                    <td>{{this.semester}}</td>
                    <td>{{this.department}}</td>
                    <td>{{this.event}}
                        <a href="/admin/edit/{{this._id}}" class="btn btn-warning btn-sm">Edit</a>
          <a href="/admin/delete/{{this._id}}" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure?')">Delete</a>
                    </td>
                    
                    
                </tr>
            {{/each}}
        </tbody>
    </table>
    <div class="text-center">
        <a href="/" class="btn btn-primary">Back to Registration</a>
    </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>



